name: E2E Tests

on:
  pull_request:
    branches: ["**"]
  push:
    branches: [main]

jobs:
  e2e:
    name: Playwright E2E (Firestore Emulator)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Install Java (required by Firebase Emulator)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Cache Firebase Emulator binaries
        uses: actions/cache@v4
        with:
          path: ~/.cache/firebase/emulators
          key: firebase-emulators-${{ hashFiles('firebase.json') }}

      # Força o download dos binários antes do exec para que o cache funcione.
      # Na segunda rodada em diante esse step é instantâneo (cache hit).
      - name: Download emulator binaries
        run: |
          npx firebase emulators:start --only firestore --project demo-project &
          EMULATOR_PID=$!
          sleep 30
          kill $EMULATOR_PID 2>/dev/null || true
        env:
          FIREBASE_TOKEN: unused

      - name: Build app (emulator mode)
        run: npm run build
        env:
          VITE_FIREBASE_USE_EMULATOR: 'true'

      # emulators:exec sobe o emulador, executa o comando e derruba — sem race condition.
      - name: Run E2E tests (inside emulator)
        run: |
          npx firebase emulators:exec \
            --only firestore \
            --project demo-project \
            "npm run test:e2e"
        env:
          CI: 'true'
          VITE_FIREBASE_USE_EMULATOR: 'true'
          FIREBASE_TOKEN: unused

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
